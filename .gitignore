# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
# Cloud platform binaries built with the makefile
cloud-platform-*

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

.DS_Store
completions
dist

# Go test output
coverage.out
